# @package _global_

# to execute this experiment run:
# python src/train.py experiment=sar_ddc_simple

defaults:
  - override /data: tsx_ssc.yaml
  - override /model: res_scale_hyperprior.yaml # res_simple_ae.yaml
  - override /callbacks: tsx_ddc.yaml
  - override /logger: wandb.yaml
  - override /trainer: gpu.yaml
  - override /hydra: default.yaml

callbacks:
  compare_recon_to_gt:
    patch_dir: ${paths.data_dir}

trainer:
  min_epochs: 1
  max_epochs: 30
  deterministic: True
  detect_anomaly: False
  log_every_n_steps: 5

logger:
  wandb:
    project: "SAR_DDC-RD-curve"
    offline: False

# The task name determines the logs path, by convention use <task>/<dataset>/<model>
task_name: train/sar_ddc/hyperprior_dpu
tags: ["TSX_SSC", "res_scale_hyperprior"]
seed: 42

# Flatten the lmbda value because W&B custom chart expressions do not support nested dicts
lambda: ${model.criterion.lmbda}
