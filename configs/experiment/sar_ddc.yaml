# @package _global_

# to execute this experiment run:
# python src/train.py experiment=sar_ddc

defaults:
  - override /data: tsx_ssc.yaml
  - override /model: sar_hyperprior.yaml
  - override /callbacks: tsx_ddc.yaml
  - override /logger: wandb.yaml
  - override /trainer: gpu.yaml
  - override /hydra: default.yaml

data:
  hdf5_dir: ${paths.data_dir}processed_hdf5/randomsplit5_nopres_norm0nat
  log_mode: "natural"
  normalize: 0 # 0 <=> true min-max, 1 <=> min-max with 1% and 99% percentiles, etc.
  batch_size: 16
  num_workers: 11

model:
  criterion:
    lmbda: 0.1

  net_optimizer:
    lr: 0.0001 # Got infinite loss with 0.001

callbacks:
  log_reconstruction:
    log_every_n_epochs: 1
    num_images: 2
    add_residuals: True
    verbose: False
  early_stopping:
    verbose: True

trainer:
  min_epochs: 1
  max_epochs: 50

logger:
  wandb:
    project: "sar-ddc-test"
    offline: False
    # tags: ${tags}
    # name: ${name}

# The task name determines the logs path, by convention use <task>/<dataset>/<model>
task_name: train/sar_ddc/hyperprior
tags: ["TSX_SSC", "hyperprior"]
seed: 42
